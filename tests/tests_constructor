from selenium import webdriver
from selenium.webdriver.common.by import By

driver = webdriver.Chrome() # переход по разделу «Соусы»

driver.get("https://stellarburgers.education-services.ru/")

driver.find_element(By.XPATH, "//span[text()='Соусы']").click()

element = driver.find_element(By.XPATH, "//h2[text()='Соусы']")
assert element.is_displayed()

driver.quit()

driver = webdriver.Chrome() # переход по разделу «Начинки»

driver.get("https://stellarburgers.education-services.ru/")

driver.find_element(By.XPATH, "//span[text()='Начинки']").click()

element = driver.find_element(By.XPATH, "//h2[text()='Начинки']")
assert element.is_displayed()

driver.quit()

driver = webdriver.Chrome() # переход по разделу «Булки»

driver.get("https://stellarburgers.education-services.ru/")

driver.find_element(By.XPATH, "//span[text()='Начинки']").click()
driver.find_element(By.XPATH, "//span[text()='Булки']").click()

element = driver.find_element(By.XPATH, "//h2[text()='Булки']")
assert element.is_displayed()

driver.quit()